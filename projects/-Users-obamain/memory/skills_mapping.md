---
name: Skills to Projects Mapping
description: Maps Claude Code skills to specific projects where they apply — avoids re-exploring projects to rediscover this
type: reference
---

# Claude Code Skills → Projects

Auto-maintained by `~/.claude/hooks/update-skills-memory.sh` when a new skill is added.

## endurance-app (`~/projects/endurance-app/`)
- `/validate` — stress-test the runner rehab wedge assumption before building
- `/schedule` — recurring Linear issue checker for overdue tasks
- `/loop` — poll Linear during active validation sessions

## ready-for-agents (`~/projects/ready-for-agents/`)
- `/schedule` — weekly podcast pipeline report: checks raw files, flags distribution gaps
- `/loop` — checklist reminders during editing sessions
- `/simplify` — use after editing `make_thumbnail.py`
- `/claude-api` — build agents for auto-generating show notes or thumbnail text from transcripts
